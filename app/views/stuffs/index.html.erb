<h1>Listing stuffs</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Interests</th>
      <th>Beer of choice</th>
      <th>Favorite cheese</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @stuffs.each do |stuff| %>
      <tr>
        <td><%= stuff.name %></td>
        <td><%= stuff.interests %></td>
        <td><%= stuff.beer_of_choice %></td>
        <td><%= stuff.favorite_cheese %></td>
        <td><%= link_to 'Show', stuff %></td>
        <td><%= link_to 'Edit', edit_stuff_path(stuff) %></td>
        <td><%= link_to 'Destroy', stuff, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Stuff', new_stuff_path %>
